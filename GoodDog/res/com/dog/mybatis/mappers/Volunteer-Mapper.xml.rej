diff a/GoodDog/res/com/dog/mybatis/mappers/Volunteer-Mapper.xml b/GoodDog/res/com/dog/mybatis/mappers/Volunteer-Mapper.xml	(rejected hunks)
@@ -25,11 +25,13 @@
 		from VOL_WANT
 	</select>
 
-	<select id="insertVolWantMember" resultType="volunteer">
-		select
-		*
-		from VOL_WANT
-	</select>
+	<insert id="insertVolWantMember" >
+	
+		INSERT INTO VOL_WANT
+		( vol_no, mem_id, vol_title )
+		VALUES (vol_no_seq.nextval, #{memId}, #{volTitle})
+		
+	</insert>
 
 
 	<select resultType="volunteer" id="selectVolList">
